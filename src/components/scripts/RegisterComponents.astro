<script>
  if (window.inEditorMode) {
    import("@cloudcannon/editable-regions/astro").then(({ registerAstroComponent }) => {
      // Register your main UI components here
      registerAstroComponent("Button", "/components/ui/Button.astro", {
        label: "Button",
      });
      
      registerAstroComponent("Section", "/components/ui/Section.astro", {
        label: "Section",
      });

      registerAstroComponent("Feature", "/components/ui/Feature.astro", {
        label: "Feature",
      });

      registerAstroComponent("Avatar", "/components/ui/Avatar.astro", {
        label: "Avatar",
      });

      registerAstroComponent("Testimonial", "/components/ui/Testimonial.astro", {
        label: "Testimonial",
      });

      registerAstroComponent("CTA", "/components/ui/CTA.astro", {
        label: "Call to Action",
      });

      registerAstroComponent("Badge", "/components/ui/Badge.astro", {
        label: "Badge",
      });

      registerAstroComponent("Highlight", "/components/ui/Highlight.astro", {
        label: "Highlight",
      });
    }).catch((error) => {
      console.warn("Failed to load CloudCannon component registration:", error);
    });
  }
</script>
